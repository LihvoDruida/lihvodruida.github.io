{
  "name": "guild-applications-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/raid-announcements.js && node --check src/raid-lifecycle.js"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
