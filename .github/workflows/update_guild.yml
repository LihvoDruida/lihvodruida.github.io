name: Update Guild Data

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.x

      - name: Install dependencies
        run: pip install requests pyyaml

      - name: Fetch Raider.IO and update YAML
        run: python scripts/update_guild.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add _data/guild.yml
          if ! git diff --cached --quiet; then
            git commit -m "chore: auto-update guild stats from Raider.IO"
            git push
          else
            echo "::error::No changes to guild data found. Workflow stopped."
            exit 1
          fi